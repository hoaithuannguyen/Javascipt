let a = +prompt("nhập số a")
let b = +prompt("nhập số b")
let c = +prompt("nhập số c")

/* if (a > b) {
    if (a > c) {
        if (b > c) {
            console.log(`${a} là max, ${c} là min`);
        } else if (b < c) {
            console.log(`${a} là max, ${b} là min`);

        }
    } else if (a < c) {
        console.log(`${c} là max, ${b} là min`);
    }
} else if (a < b) {
    if (a < c) {
        if (b > c) {
            console.log(`${b} là max, ${a} là min`);
        } else if (b < c) {
            console.log(`${c} là max, ${a} là min`);
        }
    } else if (a > c) {
        console.log(`${b} là max, ${c} là min`);

    }
}
 */

/* if (a > b) {
    if (a > c) {
        if (b > c) {
            console.log(`${a} là max, ${c} là min`);

        } else {
            console.log(`${a} là max, ${b} là min`);

        }
    } else {
        console.log(`${c} là max, ${b} là min`);

    }
} else if (a < b) {
    if (a < c) {
        if (b > c) {
            console.log(`${b} là max, ${a} là min`);
        } else {
            console.log(`${c} là max, ${a} là min`);
        }
    } else {
        console.log(`${b} là max, ${c} là min`);

    }
}
// rút gọn */